builtins.throw ''
default.nix has moved.

Use default.nix.example as a reference when wiring Sbtix into your own project.
This file is intentionally inert so running “nix build” at the repo root fails
fast instead of using an obsolete stub.
'';
